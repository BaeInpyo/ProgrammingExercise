#include <iostream>

using namespace std;

int num_tests;
int num_divisor;
int lo;
int hi;

void solution() {
  
}

int main() {
  cin.sync_with_stdio(false);

  cin >> num_tests;

  while (--num_tests >= 0) {
    cin >> num_divisor;
    cin >> lo;
    cin >> hi;

    solution();
  }

  return 0;
}

#include <iostream>

using namespace std;

int num_gallery;
int num_hall;

void solution() {}

int main() {
  cin.sync_with_stdio(false);
  int c; cin >> c;
  while (c--) {
    cin >> num_gallery >> num_hall;
    for (int i = 0; i < num_hall; ++i) {
      int a; int b;
      cin >> a >> b;
    }
    solution();
  }
  return 0;
}